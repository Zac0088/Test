
<body class='itineraryPage'>

    <div class="userInfo">
        <a class="logout" href="/views/login.handlebars">Logout</a>
        <p>Hi! {{user.username}}</p>
        <p>Email: {{user.email}}</p>

    </div>
    <div class="field">
          <label class="label">Location</label>
          <div class="control">
            <input id="autocomplete" class="input" placeholder="Add Location" type="text" />
            <div class="field is-grouped">
          <div class="control">
            <button id="searchsubmit" class="button">Submit</button>
          </div>
        </div>
          </div>
        </div>       
    <div class="savedItinerary">

        <h3> Itinerary </h3>
        
        {{#each list}}
        <form method="POST" action="/userHome/:user/:title">
            <li> <a href='/userHome/{{this.user}}/{{this.title}}'>{{this.title}}</a>
            </li>
        </form>
        {{/each}}

    </div>

<input type="date">
    <div class="itineraryEditor">
        <div class="addItinerary">
            <form method="POST" action='/userHome/:user'>
                <input type="text" name="itineraryTitle" placeholder="Itinerary Title">
                <button>Create New Itinerary</button>
            </form>
        </div>
    </div>

    <div class="itineraryLists">
        <div class="itineraryName">{{title}}</div>
        <div class="eventsLists">
            {{#each events}}
            <li>
                <form method="POST" action="/userHome/:user/:title">
                    <span class="date">{{this.date}}</span>
                    <span class = "location">{{this.location}}</span>
                    <p class="event">{{this.event}}</p>
                    <button class="deleteBtn">Delete</button>
                    <input class="userName" name='deleteEvent' value="{{this.event}}">
                </form>
            </li>
            {{/each}}
        </div>
    </div>
    <script src="/autocomplete.js"></script>
</body>

</html>


































<!-- Loop over each object in the projects array -->
{{!-- {{#each destinations as |project| }}
<div class="row mb-4 project">
  <div class="col-md-5">
    <h2>
      <!-- Call helper function with triple brackets to render the emoji span as HTML -->

      <a href="/project/{{project.id}}">{{project.name}}</a>
    </h2>
    <p>Created by {{project.user.name}} on {{format_date project.date_created}}</p>
  </div>
  <div class="col-md-7">
    <p>
      {{project.description}}
    </p>
  </div>
</div>
{{/each}} --}}